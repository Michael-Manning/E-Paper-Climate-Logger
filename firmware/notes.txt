
TODO:
- add button sequence to factory reset the EEPROM and fuel gauge
- should re-render display when time range setting is changed, or if new climate data has arrived while the user was in the menu.
- replace datetime version that only has hour/minute/second. Change read/write to only use those registers to save time
- can do some async i2c stuff, like when hibernating, you can hibernate the display at the same time as the eeprom is updated
- create mock EEPROM driver and create unit tests to verify data retrieval logic and ensure there are not bugs related to ring buffer edges.
- update firmware update script to send over the current time before uploading the firmware. That way you get a free clock synchronization every time you upload.
- activate fuel gauge hibernation mode when powering off

Bugs:
- on rare occasion, the display will do a full refresh as the main menu is entered. It should only ever do partial refreshes.
- when the battery first dips below the dead threshold voltage, it can possibly bounce back above the threshold depending on power draw and temperature. This means you can potentially turn it back on few a minutes after the battery dies
    - create some sort of latch in the eerpom so that once the battery dies the device stays in it's dead state until it's actually been connected to a charger for a few minute at least.
- when staying in menus for over a minute, climate samples are stored, but the graph does not show the recent samples until the next natural wake up cycle. Fix: update global sample buffer in addition to calling AddClimateSample()
- connecting the charging cable, then it sometimes causes the device to get stuck on the charging screen until a button is pressed.
