
TODO:
- improve history view
- support viewing humidity history?
- utilize fuel gauge GPOUT (fuelGaugeGPIO)
- add button sequence to factory reset the EEPROM 
- should re-render display when time range setting is changed
- Add nighttime display setting. When enabled, shows a nighttime screen and does not update the screen during night hours to save battery. 
    - still allow user to see temperature graph for 1 minute when they wake up the device manually with the buttons.
- replace datetime version that only has hour/minute/second. Change read/write to only use those registers to save time
- can do some async i2c stuff, like when hibernating, you can hibernate the display at the same time as the eeprom is updated
- create mock EEPROM driver and create unit tests to verify data retrieval logic and ensure there are not bugs related to ring buffer edges.